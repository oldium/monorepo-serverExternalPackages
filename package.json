{
  "name": "monorepo-serverExternalPackages",
  "description": "",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/common",
    "packages/server",
    "packages/ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @my-project/ui",
    "build": "cross-env NODE_ENV=production npm run build --workspaces"
  },
  "dependencies": {
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
